import { Client, Typing } from "discord.js";

export default function (typing: Typing) {
    /* if (typing.user.bot) return;
    typing.channel.send(`What are you writing there? <@${typing.user.id}>`) */
}